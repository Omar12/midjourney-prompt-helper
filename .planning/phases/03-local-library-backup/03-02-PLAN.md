---
phase: 03-local-library-backup
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/ui/LibraryDrawer/PromptEntryCard.tsx
  - src/ui/LibraryDrawer/LibraryDrawer.tsx
autonomous: true
requirements:
  - LIB-03
  - LIB-04

must_haves:
  truths:
    - "User can click 'Reload prompt' on a saved entry and the full builder state is restored (intent, chips, flags, version)"
    - "If the builder has content when Reload is clicked, an AlertDialog asks for confirmation before replacing"
    - "If the builder is empty, Reload happens instantly with no dialog"
    - "User can click the Delete button on an entry and an AlertDialog confirms before permanent deletion"
    - "After deletion the entry disappears from the list without a page reload (useLiveQuery reactivity)"
    - "User can click an entry name to rename it inline; Enter commits, Escape cancels, empty string reverts to original name"
  artifacts:
    - path: "src/ui/LibraryDrawer/PromptEntryCard.tsx"
      provides: "Per-entry card: name display/rename, timestamp, Reload and Delete actions"
      exports: ["PromptEntryCard"]
    - path: "src/ui/LibraryDrawer/LibraryDrawer.tsx"
      provides: "Updated drawer using PromptEntryCard list, reload-if-dirty AlertDialog, dirty-check via Zustand"
      exports: ["LibraryDrawer"]
  key_links:
    - from: "PromptEntryCard onReload callback"
      to: "LibraryDrawer handleReload"
      via: "prop — dirty-check logic lives in LibraryDrawer, card only signals intent"
      pattern: "onReload"
    - from: "LibraryDrawer handleReload"
      to: "src/domain/library/snapshot.ts entryToSession()"
      via: "import and call; result spread into useBuildSession.setState()"
      pattern: "entryToSession|setState"
    - from: "PromptEntryCard delete AlertDialog"
      to: "src/persistence/db.ts dexieAdapter.deleteEntry(id)"
      via: "AlertDialogAction onClick"
      pattern: "dexieAdapter\\.deleteEntry"
    - from: "PromptEntryCard rename"
      to: "src/persistence/db.ts dexieAdapter.renameEntry(id, name)"
      via: "onBlur / Enter key handler"
      pattern: "dexieAdapter\\.renameEntry"
---

<objective>
Complete the CRUD interactions for saved prompts: full builder-state restore on reload with a confirm-if-dirty gate, entry deletion with confirmation, and inline rename. This plan upgrades the plain-text entry list from Plan 03-01 to a fully interactive PromptEntryCard list.

Purpose: LIB-03 (reload as editable state) is the headline capability of Phase 3 — the distinction from merely storing the rendered string. LIB-04 (delete) closes the list management loop.

Output: PromptEntryCard component with Reload / Delete / inline-rename; LibraryDrawer upgraded to use cards, pass dirty-check reload logic, and close on reload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-local-library-backup/03-CONTEXT.md
@.planning/phases/03-local-library-backup/03-PATTERNS.md
@.planning/phases/03-local-library-backup/03-UI-SPEC.md
@.planning/phases/03-local-library-backup/03-01-SUMMARY.md
</context>

<interfaces>
From src/domain/library/snapshot.ts (created in 03-01):
  export function sessionToEntry(session: Pick(BuildSessionState, 'intent'|'chips'|'selectedVersionId'|'flagValues'|'setFlags'>, name: string): LibraryEntry
  export function entryToSession(entry: LibraryEntry): Pick(BuildSessionState, 'intent'|'chips'|'selectedVersionId'|'flagValues'|'setFlags')

From src/persistence/db.ts (created in 03-01):
  export const dexieAdapter: StorageAdapter
  // StorageAdapter: saveEntry, getAllEntries, deleteEntry, renameEntry

From src/domain/library/schema.ts (created in 03-01):
  export type LibraryEntry — includes id, name, intent, chips, flags, selectedVersionId, schemaVersion, createdAt, updatedAt
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create PromptEntryCard with inline rename and delete confirmation</name>
  <files>
    src/ui/LibraryDrawer/PromptEntryCard.tsx
  </files>
  <read_first>
    - src/ui/ClearDialog.tsx — AlertDialogTrigger render= pattern (lines 41–66): use `render={Button variant="ghost"}` not asChild (Base UI polymorphism — Phase 1 D-11 established that asChild causes nested button HTML violation). Destructive AlertDialogAction className: "bg-destructive text-destructive-foreground hover:bg-destructive/90". AlertDialogCancel label pattern.
    - src/ui/ControlsPane/ChipArea.tsx — per-item action-button structural pattern (lines 12–34): flex-row with icon buttons per item; apply to entry action row.
    - .planning/phases/03-local-library-backup/03-PATTERNS.md — "src/ui/LibraryDrawer/PromptEntryCard.tsx" section: exact prop interface, isRenaming state machine, handleRenameCommit fallback-to-original logic, delete AlertDialog copy, Lucide RotateCcw/Trash2 icons, onReload prop lifted to LibraryDrawer.
    - .planning/phases/03-local-library-backup/03-UI-SPEC.md — PromptEntryCard Layout section: flex flex-col gap-1 p-3 rounded-md bg-card border border-border; action row flex gap-2 justify-between; name is Label role (text-sm font-medium truncate) with title attribute; timestamp format "Saved {Mon D} at {H:MM}"; Delete button is icon-only (aria-label, text-destructive). Reload confirm: NON-destructive styling (D-05 — neutral replacement).
    - src/domain/library/schema.ts — LibraryEntry type (created in 03-01): need id, name, createdAt for the card.
  </read_first>
  <action>
    Create `src/ui/LibraryDrawer/PromptEntryCard.tsx` as a named export PromptEntryCard.

    Props interface: `{ entry: LibraryEntry; onReload: (entry: LibraryEntry) => void }`. The dirty-check and reload AlertDialog live in LibraryDrawer; PromptEntryCard only signals reload intent via the callback. Delete AlertDialog and rename logic are self-contained in the card.

    State: isRenaming (boolean, false), draftName (string, entry.name).

    Inline rename: clicking the name span (display state) sets isRenaming=true. When isRenaming, render Input from '@/components/ui/input' with value=draftName, onChange updates draftName, onBlur calls handleRenameCommit, onKeyDown handles Enter (commit) and Escape (cancel — reset draftName to entry.name, setIsRenaming(false)), autoFocus=true. handleRenameCommit: finalName = draftName.trim() || entry.name (empty → revert to original, D-03 fallback); call dexieAdapter.renameEntry(entry.id, finalName); setIsRenaming(false).

    Timestamp: format entry.createdAt via new Date(entry.createdAt).toLocaleString('en-US', { month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit' }). Render as "Saved {formatted}".

    Action row (flex gap-2, justify-between): Reload button (variant="ghost" size="sm", RotateCcw icon 16px + "Reload prompt") calls onReload(entry). Delete button wrapped in AlertDialog — AlertDialogTrigger uses render={Button variant="ghost" size="sm" className="text-destructive" aria-label={`Delete ${entry.name}`}} (icon-only Trash2 16px, no visible text label per UI-SPEC). Delete AlertDialogAction: "Delete prompt" with destructive className "bg-destructive text-destructive-foreground hover:bg-destructive/90", onClick: dexieAdapter.deleteEntry(entry.id). Cancel: "Keep it". Title: "Delete this prompt?". Description: entry.name rendered as text child inside quotes — never dangerouslySetInnerHTML.

    All text (entry.name, timestamp) rendered as React children. No dangerouslySetInnerHTML anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 — PromptEntryCard has no TypeScript errors
    - `grep -c 'dangerouslySetInnerHTML' src/ui/LibraryDrawer/PromptEntryCard.tsx` returns 0
    - `grep -c 'render={' src/ui/LibraryDrawer/PromptEntryCard.tsx` returns at least 1 (AlertDialogTrigger render= pattern used, not asChild)
    - `grep -c 'onReload' src/ui/LibraryDrawer/PromptEntryCard.tsx` returns at least 2 (prop declaration + usage in button onClick)
    - `grep -c 'dexieAdapter\.deleteEntry\|dexieAdapter\.renameEntry' src/ui/LibraryDrawer/PromptEntryCard.tsx` returns 2 (both CRUD operations present)
    - `grep -c 'draftName\.trim.*entry\.name\|entry\.name.*draftName' src/ui/LibraryDrawer/PromptEntryCard.tsx` returns at least 1 (empty-name fallback present)
  </acceptance_criteria>
  <done>PromptEntryCard compiles cleanly; AlertDialogTrigger uses render= pattern; entry name, timestamp, reload button, and delete confirm dialog all present with correct copy per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade LibraryDrawer — PromptEntryCard list, reload-if-dirty gate, close on reload</name>
  <files>
    src/ui/LibraryDrawer/LibraryDrawer.tsx
  </files>
  <read_first>
    - src/ui/LibraryDrawer/LibraryDrawer.tsx — current state from 03-01: plain div list, no PromptEntryCard, no reload logic. Read the full file before modifying.
    - src/ui/ClearDialog.tsx — per-field Zustand selector pattern (lines 18–23) and hasContent dirty-check (lines 24–29). LibraryDrawer needs the same selector discipline for its own dirty check (reload-if-dirty gate, D-05). Also AlertDialog pattern for the reload confirm dialog.
    - .planning/phases/03-local-library-backup/03-PATTERNS.md — "src/ui/LibraryDrawer/LibraryDrawer.tsx" section: useLiveQuery defaultResult pattern, AlertDialog for reload confirm with NON-destructive confirm button, Sheet open/close pattern with SheetClose after reload, entryToSession wiring, useBuildSession.setState() call.
    - .planning/phases/03-local-library-backup/03-UI-SPEC.md — Reload Confirm Dialog section: Title "Replace current prompt?", body "Your current work will be replaced with the saved prompt. This cannot be undone.", Cancel "Keep current", Confirm "Load saved prompt" with DEFAULT (non-destructive) background. Delete confirm uses destructive; reload confirm does not.
    - src/domain/library/snapshot.ts — entryToSession() signature (created in 03-01): takes LibraryEntry, returns Pick of BuildSessionState fields. Used in handleReload to spread into useBuildSession.setState().
    - src/state/buildSession.ts — useBuildSession hook and setState method. Read to confirm the exact setState call signature.
  </read_first>
  <action>
    Modify `src/ui/LibraryDrawer/LibraryDrawer.tsx`. Key additions:

    Add per-field Zustand selectors for dirty-check (per D-05): one const per field — intent, chips, selectedVersionId, setFlags — using useBuildSession per-field selectors (same pattern as ClearDialog.tsx lines 18–23). hasContent = intent.trim() !== '' OR chips.length > 0 OR selectedVersionId !== null OR Object.values(setFlags).some(Boolean).

    Add pendingEntry state: useState(null as LibraryEntry | null). This tracks which entry the user clicked Reload on, pending dirty-check resolution.

    Add Sheet controlled-open state: useState(false) for isOpen. SheetTrigger sets isOpen=true. SheetContent onOpenChange syncs isOpen. (Need controlled open to programmatically close after reload.)

    handleReload(entry: LibraryEntry): if !hasContent, restore immediately (call entryToSession(entry), spread result into useBuildSession.setState(), close drawer: setIsOpen(false)). If hasContent, set pendingEntry = entry to open the reload confirm AlertDialog.

    handleConfirmReload(): restore pendingEntry via entryToSession + useBuildSession.setState(); setPendingEntry(null); setIsOpen(false) to close the drawer after reload.

    Replace plain div list items with PromptEntryCard components. Pass entry={entry} and onReload={handleReload} to each card.

    Reload confirm AlertDialog: controlled by pendingEntry !== null (open={pendingEntry !== null}). Use AlertDialogAction with DEFAULT styling (NOT destructive className) for "Load saved prompt". AlertDialogCancel: "Keep current" sets pendingEntry=null. Title: "Replace current prompt?". Description: "Your current work will be replaced with the saved prompt. This cannot be undone."

    Keep useLiveQuery with defaultResult [] and the empty state exactly as 03-01 implemented it. No SheetFooter yet — ExportImport added in 03-04.

    Import additions needed: useState from 'react'; PromptEntryCard from './PromptEntryCard'; entryToSession from '@/domain/library/snapshot'; useBuildSession from '@/state/buildSession'; AlertDialog and sub-components from '@/components/ui/alert-dialog'; LibraryEntry from '@/domain/library/schema'.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 — no regressions
    - `grep -c 'entryToSession\|useBuildSession.*setState' src/ui/LibraryDrawer/LibraryDrawer.tsx` returns at least 2 (both restore operations present)
    - `grep -c 'hasContent\|intent.*trim\|chips\.length' src/ui/LibraryDrawer/LibraryDrawer.tsx` returns at least 2 (dirty-check logic present)
    - `grep -c 'PromptEntryCard' src/ui/LibraryDrawer/LibraryDrawer.tsx` returns at least 2 (import + usage)
    - `grep -c 'dangerouslySetInnerHTML' src/ui/LibraryDrawer/LibraryDrawer.tsx` returns 0
    - `grep -c 'Load saved prompt\|Keep current' src/ui/LibraryDrawer/LibraryDrawer.tsx` returns 2 (reload dialog copy present)
    - Reload confirm button does NOT have destructive className: `grep 'Load saved prompt' src/ui/LibraryDrawer/LibraryDrawer.tsx` shows no "bg-destructive" on the same line or adjacent AlertDialogAction
  </acceptance_criteria>
  <done>LibraryDrawer shows PromptEntryCard for each entry; clicking Reload on a populated builder shows the confirm dialog; confirming restores all 5 session fields and closes the drawer; deleting an entry removes it immediately from the list; inline rename persists via Dexie.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → React state | entryToSession() maps stored LibraryEntry fields back to buildSession — data was sanitized at save time |
| User gesture → Dexie mutation | Delete and rename operations are user-initiated, confirmed via dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Entry name in delete dialog description | mitigate | entry.name rendered as React text child inside `"` quotes — never dangerouslySetInnerHTML; enforced in acceptance criteria grep |
| T-03-06 | Tampering | entryToSession() mapping stored entry into buildSession | accept | Entry was produced by sessionToEntry() from already-sanitized buildSession state (sanitize() at addChip chokepoint, Phase 1); no second sanitization needed on restore (D-06) |
| T-03-07 | Denial of Service | Reload replacing builder without confirmation | mitigate | Dirty-check hasContent gate + AlertDialog confirm required before replace; empty-builder case (no data loss possible) skips dialog as UX optimization |
| T-03-08 | Tampering | Delete without confirmation | mitigate | AlertDialog confirm required; DeleteEntry is only called inside AlertDialogAction onClick handler |
| T-03-SC | Tampering | No new npm installs in this plan | accept | No new packages — all dependencies from 03-01 already installed and audited |
</threat_model>

<verification>
Phase gate for this plan:

1. `npx tsc --noEmit` — zero errors
2. `npx vitest run` — all tests green
3. Manual smoke: save a prompt → open Library → click Reload on empty builder (instant restore, no dialog) → fill builder with content → reload a different entry → confirm dialog appears → confirm → builder has saved entry's content → drawer closed
4. Manual: delete an entry → entry disappears from list immediately
5. Manual: click entry name → inline rename input appears → type new name → Enter → entry shows new name
6. `grep -rn 'dangerouslySetInnerHTML' src/ui/LibraryDrawer/` — no results
</verification>

<success_criteria>
- LIB-03: Reloading a saved entry restores intent, chips, selectedVersionId, flagValues, and setFlags into the builder; the serializer re-derives the preview from restored state
- LIB-04: Deleting a saved entry removes it from the IndexedDB table and the live list updates reactively
- Inline rename persists to Dexie and reverts to original name on empty commit
- Reload confirm-if-dirty gate works correctly for both empty and non-empty builder states
- TypeScript clean, all tests green
</success_criteria>

<output>
Create `.planning/phases/03-local-library-backup/03-02-SUMMARY.md` when done
</output>

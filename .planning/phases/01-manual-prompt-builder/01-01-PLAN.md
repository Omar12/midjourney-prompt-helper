---
phase: 01-manual-prompt-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - vitest.config.ts
  - index.html
  - src/index.css
  - src/main.tsx
  - src/test/setup.ts
  - src/domain/prompt/model.test.ts
  - src/domain/prompt/sanitize.test.ts
  - src/domain/prompt/serialize.test.ts
  - src/ui/IntentInput.test.tsx
  - src/ui/ChipInput.test.tsx
  - src/ui/ChipArea.test.tsx
  - src/ui/LivePreview.test.tsx
  - src/ui/CopyButton.test.tsx
  - src/ui/ClearButton.test.tsx
autonomous: true
requirements:
  - BLD-01
  - BLD-03
  - BLD-04
  - BLD-05
  - BLD-06
  - BLD-07
  - ASM-01
  - ASM-02
  - ASM-03

must_haves:
  truths:
    - "Running dev server at localhost:5173 with a React app rendered in the browser"
    - "All test stub files exist; `npx vitest run` exits 0 (todos are not failures)"
    - "Tailwind v4 CSS-first config active — no tailwind.config.js, no PostCSS config"
    - "shadcn/ui initialized with zinc-dark theme and violet-700 primary override"
    - "vitest.config.ts configured: happy-dom env, globals: true, coverage include src/domain/**"
  artifacts:
    - path: "vite.config.ts"
      provides: "@tailwindcss/vite plugin + @ alias to ./src"
      contains: "@tailwindcss/vite"
    - path: "vitest.config.ts"
      provides: "test runner config with happy-dom + coverage"
      contains: "happy-dom"
    - path: "src/test/setup.ts"
      provides: "jest-dom matchers + afterEach cleanup"
      contains: "@testing-library/jest-dom/vitest"
    - path: "src/index.css"
      provides: "Tailwind v4 import + shadcn/ui CSS variables"
      contains: "@import \"tailwindcss\""
    - path: "src/domain/prompt/model.test.ts"
      provides: "ASM-02 test stubs"
    - path: "src/domain/prompt/sanitize.test.ts"
      provides: "ASM-03 test stubs"
    - path: "src/domain/prompt/serialize.test.ts"
      provides: "ASM-01 test stubs"
  key_links:
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "@tailwindcss/vite plugin processes the @import"
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles array"
---

## Phase Goal

**As an** image creator, **I want to** type a description, add style keywords, and copy a formatted Midjourney prompt to my clipboard, **so that** I can go from a vague idea to a complete, correctly-formatted prompt without memorizing Midjourney syntax.

<objective>
Scaffold the Vite + React + TypeScript project with all Phase 1 dependencies installed, Tailwind v4 and shadcn/ui configured, and the test infrastructure ready. Create all test stub files (using `test.todo()`) for every behavior in VALIDATION.md — establishing the test contract that Plans 02–04 will implement against.

Purpose: Wave 0 in VALIDATION.md terms — the project must exist with a running dev server and a passing (todos-are-not-failures) test suite before any implementation begins.
Output: Runnable dev server, `npx vitest run` exits 0, all 9 test stub files present with named `test.todo()` cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-manual-prompt-builder/01-CONTEXT.md
@.planning/phases/01-manual-prompt-builder/01-RESEARCH.md
@.planning/phases/01-manual-prompt-builder/01-VALIDATION.md
@.planning/phases/01-manual-prompt-builder/01-UI-SPEC.md
@.planning/phases/01-manual-prompt-builder/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project, install dependencies, configure Tailwind v4 and vitest</name>
  <files>package.json, vite.config.ts, vitest.config.ts, src/index.css, src/main.tsx, src/test/setup.ts</files>
  <read_first>
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md §"Standard Stack" and §"Installation" — exact package versions and install commands
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md §"Architecture Patterns" Pattern 5 — Vite 8 + Tailwind v4 config
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md "Vitest Config" code example — exact vitest.config.ts shape
    - CLAUDE.md §"Technology Stack" — locked stack; cross-check all package names and versions
  </read_first>
  <action>
    Run `npm create vite@latest . -- --template react-ts` in the project root (the directory already contains CLAUDE.md and .planning/ — Vite will scaffold into the existing directory; confirm the overwrite prompt if prompted).

    Install core runtime deps: `npm install zod zustand tailwindcss @tailwindcss/vite`.

    Install dev and test deps: `npm install -D vitest @vitest/coverage-v8 @testing-library/react @testing-library/user-event @testing-library/jest-dom happy-dom @types/node`.

    Replace the generated `vite.config.ts` with one that imports `react` from `@vitejs/plugin-react` and `tailwindcss` from `@tailwindcss/vite`, adds both to the `plugins` array, and resolves the `@` alias to `path.resolve(__dirname, './src')`.

    Create `vitest.config.ts` as a separate config file (do NOT merge with vite.config.ts). Configure: `environment: 'happy-dom'`, `globals: true`, `setupFiles: ['./src/test/setup.ts']`, `coverage.provider: 'v8'`, `coverage.include: ['src/domain/**']`. Add the `@vitejs/plugin-react` plugin and the same `@` alias so vitest resolves component imports.

    Replace `src/index.css` content with a single line: `@import "tailwindcss";` followed by a blank line for the shadcn/ui CSS variable block (Task 2 fills this in).

    Replace generated `src/main.tsx` with the minimal Vite entry that imports React, ReactDOM, the App component (from `./App` — Plan 03 will write App.tsx), and `./index.css`. Wrap in `React.StrictMode`. Do NOT import App.css (delete src/App.css if the scaffold created it).

    Create `src/test/setup.ts` with two entries: `import '@testing-library/jest-dom/vitest'` and an `afterEach(() => cleanup())` block importing `cleanup` from `@testing-library/react`. Note: the import path is `@testing-library/jest-dom/vitest` (NOT `@testing-library/jest-dom`) — required for vitest compatibility (see RESEARCH.md §"State of the Art").

    Do NOT create a PostCSS config file. Do NOT create tailwind.config.js. Tailwind v4 uses the Vite plugin only.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
    The vitest run should exit 0 (no test files exist yet so it exits cleanly). Also verify: `npm run dev` starts without errors in the terminal.
  </verify>
  <acceptance_criteria>
    - `package.json` contains `zod`, `zustand` as dependencies
    - `package.json` devDependencies contains `vitest`, `happy-dom`, `@testing-library/react`, `@testing-library/jest-dom`
    - `vite.config.ts` imports `@tailwindcss/vite` and adds it to plugins; no PostCSS config file exists
    - `vitest.config.ts` specifies `environment: 'happy-dom'` and `setupFiles: ['./src/test/setup.ts']`
    - `src/index.css` begins with `@import "tailwindcss";`
    - `src/test/setup.ts` imports from `@testing-library/jest-dom/vitest` (the `/vitest` suffix is required)
    - `npm run dev` starts the Vite dev server without errors
    - No `tailwind.config.js` or `postcss.config.*` file in project root
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui, configure dark theme, create all test stubs</name>
  <files>
    src/index.css,
    src/domain/prompt/model.test.ts,
    src/domain/prompt/sanitize.test.ts,
    src/domain/prompt/serialize.test.ts,
    src/ui/IntentInput.test.tsx,
    src/ui/ChipInput.test.tsx,
    src/ui/ChipArea.test.tsx,
    src/ui/LivePreview.test.tsx,
    src/ui/CopyButton.test.tsx,
    src/ui/ClearButton.test.tsx
  </files>
  <read_first>
    - .planning/phases/01-manual-prompt-builder/01-VALIDATION.md §"Per-Task Verification Map" — exact test case names per requirement
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md §"Validation Architecture" §"Serializer Golden-String Tests" — golden-string table structures
    - .planning/phases/01-manual-prompt-builder/01-UI-SPEC.md §"Design System" — dark mode, zinc base, violet-700 primary override
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md Pattern 5 — shadcn/ui v4.12 tw-animate-css note and @theme inline
  </read_first>
  <action>
    Run `npx shadcn@latest init`. When prompted: choose New York style, Zinc base color, enable CSS variables. This generates `components.json` and populates `src/index.css` with the `@theme inline` block and shadcn/ui CSS variables.

    After init, verify that `tw-animate-css` (NOT `tailwindcss-animate`) was installed. If `tailwindcss-animate` appears in package.json, remove it and install `tw-animate-css` instead: `npm remove tailwindcss-animate && npm install tw-animate-css`.

    Override the `--primary` CSS variable in `src/index.css` inside the dark-mode `@theme inline` block to violet-700: set `--primary` to the OKLCH value `oklch(54% 0.21 291)` (per UI-SPEC.md §"Color"). This makes the Copy button (the sole accent element) use violet instead of the zinc default.

    Run `npx shadcn@latest add button textarea input badge alert-dialog`. These five components are the complete Phase 1 component inventory per UI-SPEC.md §"Component Inventory".

    Create the following test stub files. Each file imports only `describe` and `test` from `vitest` (no component or domain imports yet — those don't exist until Plans 02–04). Each test is written as `test.todo('...')` with the exact behavior description from VALIDATION.md §"Per-Task Verification Map".

    `src/domain/prompt/model.test.ts`: describe block "PromptDraftSchema" with test.todo cases for (a) validates a valid PromptDraft, (b) rejects a chip with an empty label.

    `src/domain/prompt/sanitize.test.ts`: describe block "sanitize" with test.todo cases for: (a) collapses `::` to `:`, (b) collapses `--` to `-`, (c) collapses newlines to spaces, (d) strips leading/trailing commas, (e) correct order — `\n--` becomes ` -` (newline collapse runs before dash collapse).

    `src/domain/prompt/serialize.test.ts`: describe block "serialize" with test.todo cases for: (a) intent first then chips joined by `, `, (b) same input produces same output (deterministic), (c) disabled chips are skipped, (d) empty draft produces empty string, (e) intent with user commas is preserved as-is (opaque block, D-03).

    `src/ui/IntentInput.test.tsx`: describe block "IntentInput" with test.todo for: renders and accepts typed text.

    `src/ui/ChipInput.test.tsx`: describe block "ChipInput" with test.todo cases for: (a) Add button creates a chip from input text, (b) duplicate label is silently ignored.

    `src/ui/ChipArea.test.tsx`: describe block "ChipArea" with test.todo for: clicking ✕ removes the chip.

    `src/ui/LivePreview.test.tsx`: describe block "LivePreview" with test.todo cases for: (a) preview updates when intent changes, (b) preview updates when a chip is added.

    `src/ui/CopyButton.test.tsx`: describe block "CopyButton" with test.todo cases for: (a) calls `clipboard.writeText` with the preview text, (b) shows "Copied!" label after successful copy.

    `src/ui/ClearButton.test.tsx`: describe block "ClearButton / ClearDialog" with test.todo cases for: (a) triggers confirm dialog when intent or chips are present, (b) clearing the dialog clears state, (c) no dialog appears when builder is already empty.

    Create the `src/domain/prompt/` directory and `src/ui/` directory if they don't exist.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1</automated>
    All 9 test files are discovered. All tests show as "todo" (skipped/pending). Exit code is 0. No compilation errors.
  </verify>
  <acceptance_criteria>
    - `components.json` exists at project root (shadcn/ui initialized)
    - `src/components/ui/button.tsx`, `textarea.tsx`, `input.tsx`, `badge.tsx`, `alert-dialog.tsx` all exist (shadcn/ui components added)
    - `tw-animate-css` is in package.json (NOT `tailwindcss-animate`)
    - `src/index.css` contains `--primary` override to violet-700 OKLCH value within the dark `@theme inline` block
    - All 9 test stub files exist under `src/domain/prompt/` and `src/ui/` with named `test.todo()` cases
    - `npx vitest run` exits 0 with all todos shown and no failures
    - `npx vitest run --grep "PromptDraftSchema"` discovers the model.test.ts file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → npm registry | Package installation fetches code from public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install (all packages) | mitigate | Package Legitimacy Audit completed in 01-RESEARCH.md — all 16 packages verified on npm registry, all Approved, no [SLOP] or [SUS] packages. No blocking human checkpoint required. |
| T-01-02 | Tampering | shadcn/ui `npx shadcn@latest init` | accept | Official shadcn/ui registry only (no third-party blocks); registry safety confirmed in UI-SPEC.md §"Registry Safety". |
| T-01-03 | Spoofing | `tailwindcss-animate` vs `tw-animate-css` | mitigate | Verify after shadcn/ui init: package.json must contain `tw-animate-css`, not `tailwindcss-animate`. If wrong package present, remove and reinstall correct one. |
</threat_model>

<verification>
After completing both tasks:
- `npm run dev` renders a React app at localhost:5173 without errors
- `npx vitest run` exits 0 — all stub todos shown, zero failures
- `ls src/components/ui/` shows button.tsx, textarea.tsx, input.tsx, badge.tsx, alert-dialog.tsx
- No `tailwind.config.js` or `postcss.config.*` in project root
- `grep -r "tailwindcss-animate" package.json` returns no results
</verification>

<success_criteria>
- Project scaffolded and both `npm run dev` and `npx vitest run` complete without errors
- 9 test stub files exist covering all 22 test cases from VALIDATION.md §"Per-Task Verification Map" as test.todo() entries
- Tailwind v4 CSS-first config active with shadcn/ui zinc dark theme and violet-700 primary override
- All dep versions match RESEARCH.md §"Standard Stack" (vitest 4.x, React 19.x, Tailwind 4.3.x, shadcn/ui 4.12.x)
</success_criteria>

<output>
Create `.planning/phases/01-manual-prompt-builder/01-01-SUMMARY.md` when done.
</output>

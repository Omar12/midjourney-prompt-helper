---
phase: 01-manual-prompt-builder
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/ui/PreviewPane/CopyButton.tsx
  - src/ui/ClearDialog.tsx
  - src/ui/App.tsx
  - src/ui/CopyButton.test.tsx
  - src/ui/ClearButton.test.tsx
autonomous: false
requirements:
  - BLD-06
  - BLD-07

must_haves:
  truths:
    - "User can click Copy prompt and the exact assembled prompt string is written to the OS clipboard"
    - "After clicking Copy prompt, the button label flips to Copied! for 2 seconds then reverts"
    - "Copy prompt button is disabled when the preview string is empty"
    - "User can click Clear all and a confirmation dialog appears when intent or chips are present"
    - "Clicking Clear everything in the dialog resets intent and chips to empty"
    - "When the builder is already empty, Clear all button is disabled — no dialog appears"
    - "User can visually verify the two-pane layout, chip flow, and clipboard round-trip in the browser"
  artifacts:
    - path: "src/ui/PreviewPane/CopyButton.tsx"
      provides: "Copy button with clipboard.writeText + Copied! feedback (D-10)"
      exports: ["CopyButton"]
    - path: "src/ui/ClearDialog.tsx"
      provides: "Clear all button + AlertDialog confirm (D-11)"
      exports: ["ClearButton"]
  key_links:
    - from: "src/ui/PreviewPane/CopyButton.tsx"
      to: "navigator.clipboard.writeText"
      via: "async click handler inside onClick (must be synchronous user gesture)"
    - from: "src/ui/ClearDialog.tsx"
      to: "src/state/buildSession.ts"
      via: "useBuildSession((s) => ({ intent, chips, clearAll }))"
    - from: "src/ui/App.tsx"
      to: "src/ui/PreviewPane/CopyButton.tsx"
      via: "import + render in preview pane footer"
---

## Phase Goal

**As an** image creator, **I want to** type a description, add style keywords, and copy a formatted Midjourney prompt to my clipboard, **so that** I can go from a vague idea to a complete, correctly-formatted prompt without memorizing Midjourney syntax.

<objective>
Complete the Phase 1 walking skeleton by adding the Copy button (clipboard + "Copied!" feedback per D-10) and the Clear all button (AlertDialog confirmation per D-11). Wire both into App.tsx. Make all remaining test stubs green. Then pause for a human checkpoint to verify the visual two-pane layout and real clipboard round-trip.

Purpose: These are the final two interactions that close the user story: copy and start-over. After this plan, the full Phase 1 capability is delivered and verified.
Output: CopyButton and ClearDialog implemented and tested; human confirms the complete working app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-manual-prompt-builder/01-CONTEXT.md
@.planning/phases/01-manual-prompt-builder/01-RESEARCH.md
@.planning/phases/01-manual-prompt-builder/01-VALIDATION.md
@.planning/phases/01-manual-prompt-builder/01-UI-SPEC.md
@.planning/phases/01-manual-prompt-builder/01-03-SUMMARY.md

<interfaces>
<!-- From Plans 02–03 output -->

From src/state/buildSession.ts:
  useBuildSession — selector hook
  State shape: { intent: string; chips: Chip[]; clearAll(): void }
  Access pattern: useBuildSession((s) => ({ intent: s.intent, chips: s.chips, clearAll: s.clearAll }))

From src/domain/prompt/serialize.ts:
  serialize(draft: PromptDraft): string

CopyButton receives prop: { text: string } — the serialized preview string from App.tsx
ClearButton accesses store directly via useBuildSession selector
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CopyButton + ClearDialog — complete all remaining tests and wire into App</name>
  <files>src/ui/PreviewPane/CopyButton.tsx, src/ui/ClearDialog.tsx, src/ui/App.tsx, src/ui/CopyButton.test.tsx, src/ui/ClearButton.test.tsx</files>
  <read_first>
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md Pattern 6 — CopyButton implementation (Clipboard API user-gesture requirement, useState for copied, 2000ms revert, catch block for non-secure contexts)
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md Pattern 7 — ClearButton + AlertDialog implementation (exact shadcn/ui component names: AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction)
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md Pitfall 5 — navigator.clipboard mock for tests: `Object.defineProperty(navigator, 'clipboard', { value: { writeText: vi.fn().mockResolvedValue(undefined) }, writable: true })`
    - .planning/phases/01-manual-prompt-builder/01-UI-SPEC.md §"Interaction Contract — CopyButton" — variant="default" (accent fill), disabled when empty, D-10 behavior
    - .planning/phases/01-manual-prompt-builder/01-UI-SPEC.md §"Interaction Contract — ClearButton + ClearDialog" — ghost variant, disabled when empty, dialog copy strings
    - .planning/phases/01-manual-prompt-builder/01-UI-SPEC.md §"Copywriting Contract" — exact copy: "Copy prompt", "Copied!", "Clear all", "Start over?", "This will clear your intent and all chips. This action cannot be undone.", "Keep editing", "Clear everything"
    - .planning/phases/01-manual-prompt-builder/01-CONTEXT.md D-10 (locked), D-11 (locked)
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md §"Don't Hand-Roll" — use shadcn/ui AlertDialog, NOT window.confirm() (blocked in Tauri)
  </read_first>
  <behavior>
    CopyButton tests:
    - navigator.clipboard.writeText is mocked; render CopyButton with text="a cat"; click Copy prompt; assert writeText was called with "a cat"
    - After click, button label shows "Copied!" immediately
    - After 2000ms (use fake timers via vitest's vi.useFakeTimers), label reverts to "Copy prompt"
    - CopyButton is disabled when text prop is empty string

    ClearButton tests:
    - When store has intent="a cat" and chips are empty: clicking "Clear all" button opens the AlertDialog (the dialog title "Start over?" is visible)
    - Clicking "Clear everything" in the dialog calls clearAll; store intent becomes ''
    - When store is empty (intent='', chips=[]): "Clear all" button is disabled; no dialog opens on click
  </behavior>
  <action>
    First update the two test stubs, then implement (RED→GREEN).

    `src/ui/CopyButton.test.tsx`: Replace todos with real tests. Add clipboard mock in a `beforeEach` using `Object.defineProperty(navigator, 'clipboard', { value: { writeText: vi.fn().mockResolvedValue(undefined) }, writable: true })`. Use `vi.useFakeTimers()` + `vi.advanceTimersByTime(2000)` wrapped in `act()` to test the label revert. Import and render `CopyButton` with a `text` prop. Run — FAIL (RED).

    `src/ui/ClearButton.test.tsx`: Replace todos. Use `useBuildSession.setState` to set up state. Render `ClearButton`. For the dialog-opens test: click "Clear all", then assert `screen.getByText('Start over?')` is in document. For the confirm test: click "Clear everything", assert `useBuildSession.getState().intent === ''`. For empty-state test: set state to empty, assert button has `disabled` attribute. Run — FAIL (RED).

    Create `src/ui/PreviewPane/CopyButton.tsx`:
    - Props: `{ text: string }`
    - Local state: `const [copied, setCopied] = useState(false)`
    - `handleCopy`: async function called in `onClick`. Must be a direct click handler (NOT in a timeout or effect — Clipboard API requires synchronous user gesture). Body: `if (!text) return; try { await navigator.clipboard.writeText(text); setCopied(true); setTimeout(() => setCopied(false), 2000); } catch { /* silent fallback — per UI-SPEC copy failure is silent in Phase 1 */ }`
    - Render: `<Button variant="default" onClick={handleCopy} disabled={!text} className="w-full">{copied ? 'Copied!' : 'Copy prompt'}</Button>` (UI-SPEC: variant="default" = accent fill; copy: "Copy prompt" / "Copied!" per D-10)

    Create `src/ui/ClearDialog.tsx`:
    - Export `ClearButton` component (named export, not default)
    - Import `useBuildSession` from `@/state/buildSession`
    - Import all AlertDialog pieces from `@/components/ui/alert-dialog`
    - Import `Button` from `@/components/ui/button`
    - Selector: `const { intent, chips, clearAll } = useBuildSession((s) => ({ intent: s.intent, chips: s.chips, clearAll: s.clearAll }))`
    - `const hasContent = intent.trim() !== '' || chips.length > 0`
    - Per D-11: when `!hasContent`, render `<Button variant="ghost" disabled>Clear all</Button>` and return (no dialog, instant no-op)
    - When `hasContent`: render the full `<AlertDialog>` with `<AlertDialogTrigger asChild><Button variant="ghost">Clear all</Button></AlertDialogTrigger>` and `<AlertDialogContent>` containing:
      - `<AlertDialogHeader><AlertDialogTitle>Start over?</AlertDialogTitle><AlertDialogDescription>This will clear your intent and all chips. This action cannot be undone.</AlertDialogDescription></AlertDialogHeader>`
      - `<AlertDialogFooter><AlertDialogCancel>Keep editing</AlertDialogCancel><AlertDialogAction onClick={clearAll} className="bg-destructive text-destructive-foreground hover:bg-destructive/90">Clear everything</AlertDialogAction></AlertDialogFooter>`
    - Do NOT use `window.confirm()` — blocked in Tauri webviews (RESEARCH.md §"Don't Hand-Roll")

    Also: the `addChip` action in `buildSession.ts` should sanitize the label before storing. Update `buildSession.ts` addChip to call `sanitize(trimmed)` (import `sanitize` from `'../domain/prompt/sanitize'`) before the dedup check and before pushing. This ensures chip.label in the store is already sanitized, so the aria-label in ChipArea and any downstream Phase 4 AI labels are clean. Note: `sanitize` is a pure function with no side effects — safe to call in the store action.

    Update `src/ui/App.tsx`:
    - Replace the CopyButton placeholder with `<CopyButton text={preview} />`
    - Replace the ClearButton TODO placeholder with `<ClearButton />`
    - Import both from their respective paths

    Run all 5 pending test files to GREEN (CopyButton, ClearButton; re-run ChipArea to confirm sanitize wiring doesn't break it).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
    Full suite exits 0. All 9 test files pass. Zero test.todo() remaining across the entire suite. Zero failures.
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 — all 9 test files, all 22 test cases passing, zero todos
    - `CopyButton.tsx` calls `navigator.clipboard.writeText(text)` inside a synchronous click handler (NOT in useEffect or Promise.then chain)
    - Button shows "Copied!" after click and reverts to "Copy prompt" after 2000ms
    - `ClearDialog.tsx` uses `AlertDialog` from shadcn/ui (never `window.confirm()`)
    - `ClearButton` is `disabled` and shows no dialog when `!hasContent`
    - `buildSession.ts` addChip calls `sanitize(trimmed)` before storing the label
    - App.tsx renders CopyButton and ClearButton (no TODO placeholder comments remain)
    - `grep -rn "window.confirm" src/` returns zero results
    - `grep -rn "dangerouslySetInnerHTML" src/` returns zero results
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 1 Manual Prompt Builder: two-pane layout with intent textarea, chip add/remove,
    live prompt preview, copy to clipboard, and clear/reset with confirmation. All automated
    tests pass (npx vitest run exits 0, 22 assertions green). Walking skeleton complete.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:5173. Then verify each item:

    1. LAYOUT — Wide viewport (> 768px): controls pane on the left (flex-1), preview pane on the right (fixed w-96, sticky). Preview pane stays visible while controls scroll.

    2. LAYOUT — Narrow viewport (< 768px): resize browser to 640px wide. Panes should stack: controls on top, preview below. Confirm preview is still reachable by scrolling.

    3. INTENT INPUT — Type "a misty mountain lake at dawn, shot from above" into the textarea. Confirm the preview pane updates immediately to show the same text (no Midjourney injection characters in this text, so output should match input).

    4. CHIP ADD — Type "cinematic" in the chip input, press Enter. Confirm a "cinematic" badge appears in the chip area. Confirm the preview now reads: "a misty mountain lake at dawn, shot from above, cinematic".

    5. CHIP ADD — Type "cinematic" again, press Enter or click Add keyword. Confirm no duplicate "cinematic" chip appears (dedup is silent).

    6. CHIP ADD — Add "neon" and "oil painting" chips. Confirm preview reads: "a misty mountain lake at dawn, shot from above, cinematic, neon, oil painting".

    7. CHIP REMOVE — Click ✕ on "neon". Confirm "neon" badge disappears and preview updates.

    8. SPECIAL CHARS — Clear the intent field. Type "a--b::c" into intent. Confirm preview shows "a-b:c" (double-dash → single dash, double-colon → single colon; sanitizer active).

    9. COPY BUTTON — Build a prompt with intent and at least one chip. Click "Copy prompt". Confirm button flips to "Copied!" immediately. Paste into any text editor — confirm the exact prompt string was copied (format: "intent, chip1, chip2").

    10. COPY DISABLED — Clear intent and all chips so preview is empty. Confirm "Copy prompt" button is disabled (grayed out, not clickable).

    11. CLEAR WITH CONTENT — Type intent text and add a chip. Click "Clear all". Confirm AlertDialog opens with title "Start over?" and body "This will clear your intent and all chips. This action cannot be undone."

    12. CLEAR CANCEL — Click "Keep editing" in the dialog. Confirm dialog closes and content is preserved.

    13. CLEAR CONFIRM — Click "Clear all" again. Click "Clear everything". Confirm intent textarea is empty, chip area is empty, preview shows "Your prompt will appear here…", and "Clear all" button is now disabled.

    14. CLEAR EMPTY — With builder empty, click "Clear all". Confirm no dialog opens (button should be disabled — should be unclickable).

    15. PREVIEW EMPTY STATE — Confirm when builder is empty, the preview pane shows "Your prompt will appear here…" in muted text color.
  </how-to-verify>
  <resume-signal>
    Type "approved" when all 15 items pass.
    Or describe any item that failed (include item number and what you observed vs. what was expected).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → navigator.clipboard | The assembled prompt string exits the app to the OS clipboard at this boundary |
| User input → store.addChip → sanitize() | Chip label is sanitized before storage (added in this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | CopyButton — clipboard permission denied | accept | `try/catch` in handleCopy swallows the error silently per UI-SPEC §"CopyButton" — Phase 1 has no error state for this; desktop (Tauri Phase 5) can add a fallback. Risk is low: localhost dev + HTTPS prod always have clipboard access. |
| T-04-02 | Tampering | addChip sanitize gate — chip label bypasses sanitize() | mitigate | `buildSession.ts` addChip now calls `sanitize(trimmed)` before storing; any future code path that adds chips must route through `addChip` to inherit the sanitize gate. Phase 4 AI content will use the same addChip action. |
| T-04-03 | Tampering | window.confirm() in Tauri | mitigate | AlertDialog (shadcn/ui Radix) used instead of window.confirm() — confirmed blocked in Tauri webviews per RESEARCH.md §"Don't Hand-Roll". |
| T-04-04 | Information Disclosure | Clipboard — prompt content exposed to other apps | accept | This is intentional product behavior (user copies to paste into Midjourney). No secrets in Phase 1 prompts. Clipboard content is ephemeral. |
| T-04-SC | Tampering | npm/shadcn — no new installs this plan | accept | No new packages installed. All packages from Plan 01 audit. |
</threat_model>

<verification>
After Task 1 (automated):
- `npx vitest run` exits 0 — ALL 9 test files pass, 22 total assertions green
- `npx vitest run --coverage` — src/domain/** at 100% branch coverage
- Zero `window.confirm` in src/
- Zero `dangerouslySetInnerHTML` in src/
- `grep -n "sanitize" src/state/buildSession.ts` shows sanitize import and call in addChip

After Task 2 (human checkpoint):
- All 15 manual verification items approved
- Real clipboard round-trip confirmed (paste from Midjourney prompt builder matches expected string)
</verification>

<success_criteria>
- Full automated test suite green: `npx vitest run` exits 0, all 9 test files, all 22 cases
- Domain coverage: `npx vitest run --coverage` — src/domain/** 100% branch coverage
- CopyButton writes to clipboard inside a synchronous click handler; shows "Copied!" for 2s
- ClearDialog uses shadcn/ui AlertDialog (not window.confirm); disabled when builder is empty
- addChip sanitizes labels before storage — the sanitize() chokepoint (D-08) is now fully active
- Human verifies all 15 items: layout, intent, chips, sanitization, copy, clear, empty states
- Walking skeleton capability proven: user goes from blank → intent + chips → preview → clipboard copy in < 30 seconds
</success_criteria>

<output>
Create `.planning/phases/01-manual-prompt-builder/01-04-SUMMARY.md` when done.
</output>

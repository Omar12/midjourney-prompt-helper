---
phase: 01-manual-prompt-builder
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/domain/prompt/model.ts
  - src/domain/prompt/sanitize.ts
  - src/domain/prompt/serialize.ts
  - src/domain/prompt/model.test.ts
  - src/domain/prompt/sanitize.test.ts
  - src/domain/prompt/serialize.test.ts
autonomous: true
requirements:
  - ASM-01
  - ASM-02
  - ASM-03

must_haves:
  truths:
    - "PromptDraftSchema validates a complete draft object and rejects invalid shapes"
    - "`sanitize('a::b')` returns `'a:b'` and `sanitize('a--b')` returns `'a-b'`"
    - "`sanitize('\\n--ar')` returns `' -ar'` (newlines collapse before dash collapse)"
    - "`serialize(draft)` returns intent first, then chips joined by `, `"
    - "`serialize()` is deterministic: same PromptDraft input always produces the same string"
    - "Disabled chips are excluded from the serialized output"
  artifacts:
    - path: "src/domain/prompt/model.ts"
      provides: "PromptDraft, Chip, FlagValue Zod schemas and inferred TS types"
      exports: ["ChipSchema", "FlagValueSchema", "PromptDraftSchema", "Chip", "FlagValue", "PromptDraft"]
    - path: "src/domain/prompt/sanitize.ts"
      provides: "sanitize(text: string): string — single chokepoint utility"
      exports: ["sanitize"]
    - path: "src/domain/prompt/serialize.ts"
      provides: "serialize(draft: PromptDraft): string — deterministic pure function"
      exports: ["serialize"]
  key_links:
    - from: "src/domain/prompt/serialize.ts"
      to: "src/domain/prompt/sanitize.ts"
      via: "import { sanitize } from './sanitize'"
      pattern: "sanitize(draft.intent"
    - from: "src/domain/prompt/serialize.ts"
      to: "src/domain/prompt/model.ts"
      via: "import type { PromptDraft } from './model'"
---

## Phase Goal

**As an** image creator, **I want to** type a description, add style keywords, and copy a formatted Midjourney prompt to my clipboard, **so that** I can go from a vague idea to a complete, correctly-formatted prompt without memorizing Midjourney syntax.

<objective>
Implement the three domain core files: the `PromptDraft` Zod schema (the canonical re-editable model), the `sanitize()` single-chokepoint utility (escaping MJ-syntax-breaking characters), and the `serialize()` deterministic pure function (model → prompt string). Update the three domain test stubs from Plan 01 to real table-driven assertions and make them pass (RED→GREEN).

Purpose: Establish the stable integration surface that Phases 2, 3, and 4 extend — the `PromptDraft` shape, the serializer contract (D-01..D-04), and the sanitization chokepoint (D-05..D-08). These are pure TypeScript with no framework deps; test them in isolation.
Output: `src/domain/prompt/model.ts`, `sanitize.ts`, `serialize.ts` implemented and all three domain test files green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-manual-prompt-builder/01-CONTEXT.md
@.planning/phases/01-manual-prompt-builder/01-RESEARCH.md
@.planning/phases/01-manual-prompt-builder/01-VALIDATION.md

<interfaces>
<!-- Domain contracts that Plans 03–04 will import. Defined here. -->

src/domain/prompt/model.ts will export:
  ChipSchema — Zod object with: id (uuid), label (string min 1 max 200), source (enum: 'custom' | 'palette'), paletteCategory (string optional), enabled (boolean default true)
  FlagValueSchema — Zod object with: flagId (string), value (unknown)
  PromptDraftSchema — Zod object with: id (uuid), intent (string max 2000), chips (ChipSchema array), flags (FlagValueSchema array), schemaVersion (literal 1), createdAt (datetime string), updatedAt (datetime string)
  Chip — inferred from ChipSchema
  FlagValue — inferred from FlagValueSchema
  PromptDraft — inferred from PromptDraftSchema

src/domain/prompt/sanitize.ts will export:
  sanitize(text: string): string

src/domain/prompt/serialize.ts will export:
  serialize(draft: PromptDraft): string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PromptDraft Zod schema (ASM-02) and make model.test.ts green</name>
  <files>src/domain/prompt/model.ts, src/domain/prompt/model.test.ts</files>
  <read_first>
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md §"Architecture Patterns" Pattern 1 — exact PromptDraftSchema shape, forward-compatibility guarantees (flags array, source discriminator, schemaVersion literal)
    - .planning/phases/01-manual-prompt-builder/01-CONTEXT.md §"Claude's Discretion" — PromptDraft model shape notes (intent as top-level field, not a chip)
    - .planning/phases/01-manual-prompt-builder/01-VALIDATION.md §"Per-Task Verification Map" rows for ASM-02 — test case names to implement
  </read_first>
  <behavior>
    - PromptDraftSchema.parse({ id: crypto.randomUUID(), intent: 'a cat', chips: [], flags: [], schemaVersion: 1, createdAt: new Date().toISOString(), updatedAt: new Date().toISOString() }) succeeds
    - PromptDraftSchema.parse with a chip where label is '' throws a ZodError
    - ChipSchema.parse({ id: crypto.randomUUID(), label: 'cinematic', source: 'custom', enabled: true }) succeeds
    - ChipSchema.parse({ id: 'not-a-uuid', label: 'test', source: 'custom', enabled: true }) throws ZodError (id must be UUID)
    - PromptDraftSchema.parse with schemaVersion: 2 throws ZodError (literal 1 only)
  </behavior>
  <action>
    First update `src/domain/prompt/model.test.ts`: replace all `test.todo()` stubs with real assertions using `describe`, `test`, and `expect`. Import `PromptDraftSchema` and `ChipSchema` from `./model`. Implement the golden-string table from RESEARCH.md §"Validation Architecture". Run tests — they should FAIL (RED) because model.ts does not exist yet.

    Then create `src/domain/prompt/model.ts` implementing the schema exactly as described in RESEARCH.md Pattern 1. Key requirements:
    - `import { z } from 'zod'`
    - `ChipSchema`: fields id (z.string().uuid()), label (z.string().min(1).max(200)), source (z.enum(['custom', 'palette'])), paletteCategory (z.string().optional()), enabled (z.boolean().default(true))
    - `FlagValueSchema`: fields flagId (z.string()), value (z.unknown())
    - `PromptDraftSchema`: fields id (z.string().uuid()), intent (z.string().max(2000)), chips (z.array(ChipSchema)), flags (z.array(FlagValueSchema)), schemaVersion (z.literal(1)), createdAt (z.string().datetime()), updatedAt (z.string().datetime())
    - Export inferred types: `Chip`, `FlagValue`, `PromptDraft`

    Forward-compatibility: the `source: z.enum(['custom', 'palette'])` discriminator is in place from Phase 1 so Phase 4 can add palette chips without a schema reshape. The `flags: z.array(FlagValueSchema)` array is in place so Phase 2 can populate it without a reshape. The `schemaVersion: z.literal(1)` enables future migration detection.

    Run tests after implementation — they should pass (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/domain/prompt/model.test.ts --reporter=verbose</automated>
    All tests green. Zero todos remaining in this file.
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/prompt/model.test.ts` exits 0 with all tests passing
    - `model.ts` exports `ChipSchema`, `FlagValueSchema`, `PromptDraftSchema`, `Chip`, `FlagValue`, `PromptDraft`
    - `model.ts` has zero framework imports (no React, no Zustand) — pure Zod only
    - `ChipSchema` source field uses `z.enum(['custom', 'palette'])` — both values present from Phase 1
    - `PromptDraftSchema` flags field is `z.array(FlagValueSchema)` — not optional, defaults to empty array
    - `schemaVersion: z.literal(1)` — exact literal, not z.number()
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sanitize() and serialize() and make all domain tests green</name>
  <files>src/domain/prompt/sanitize.ts, src/domain/prompt/serialize.ts, src/domain/prompt/sanitize.test.ts, src/domain/prompt/serialize.test.ts</files>
  <read_first>
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md Pattern 2 — sanitize() implementation and EXACT operation order (newlines first, then ::, then --, then strip leading/trailing commas). Order matters — see Pitfall 3.
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md Pattern 3 — serialize() implementation and determinism guarantees
    - .planning/phases/01-manual-prompt-builder/01-CONTEXT.md §"Special-Character Escaping" D-05..D-08 — locked decisions
    - .planning/phases/01-manual-prompt-builder/01-CONTEXT.md §"Prompt String Format" D-01..D-04 — serializer contract
    - .planning/phases/01-manual-prompt-builder/01-VALIDATION.md §"Serializer Golden-String Tests" — the exact table-driven test cases to implement
    - .planning/phases/01-manual-prompt-builder/01-RESEARCH.md Pitfall 3 — sanitizer order warning
  </read_first>
  <behavior>
    sanitize():
    - sanitize('a::b') === 'a:b'
    - sanitize('a--b') === 'a-b'
    - sanitize('--style') === '-style'
    - sanitize('a\nb') === 'a b'
    - sanitize('a\r\nb') === 'a b'
    - sanitize(',leading') === 'leading'
    - sanitize('trailing,') === 'trailing'
    - sanitize('\n--ar 1:1') === ' -ar 1:1' (newline → space first, THEN -- → -)
    - sanitize('::weight') === ':weight'
    - sanitize('') === ''

    serialize():
    - serialize({ intent: 'a cat', chips: [], flags: [] }) === 'a cat'
    - serialize({ intent: 'a cat', chips: [{ label: 'cinematic', enabled: true }] }) === 'a cat, cinematic'
    - serialize({ intent: 'a cat', chips: [{ label: 'cinematic', enabled: true }, { label: 'hazy', enabled: true }] }) === 'a cat, cinematic, hazy'
    - serialize({ intent: '', chips: [{ label: 'cinematic', enabled: true }] }) === 'cinematic'
    - serialize({ intent: '', chips: [] }) === ''
    - serialize with a chip where enabled: false — that chip is excluded from output
    - serialize({ intent: 'cats, dogs', chips: [] }) === 'cats, dogs' (user commas in intent preserved — D-03 opaque block)
    - Same PromptDraft object passed twice produces identical string output (determinism D-04)
  </behavior>
  <action>
    First update both test files to replace all `test.todo()` stubs with real assertions.

    `src/domain/prompt/sanitize.test.ts`: Implement table-driven tests using a `const cases` array of `{ input, expected }` objects. Use `test.each` or a forEach loop. Import `sanitize` from `./sanitize`. Run — tests FAIL (RED, file doesn't exist).

    `src/domain/prompt/serialize.test.ts`: Create a `mkDraft` helper function inside the test file that constructs a minimal valid PromptDraft from (intent: string, chipLabels: string[], disabledLabels?: string[]). Import `serialize` from `./serialize` and `PromptDraft` type from `./model`. Implement the golden-string cases from RESEARCH.md §"Serializer Golden-String Tests" plus the determinism test (call serialize twice with the same object, assert equality). Run — tests FAIL (RED).

    Then create `src/domain/prompt/sanitize.ts`:
    Export function `sanitize(text: string): string` that applies operations in this EXACT order (per D-06, D-07, Pitfall 3):
    1. `replace(/\r\n|\r|\n/g, ' ')` — all newline variants → single space
    2. `replace(/::/g, ':')` — double-colon → colon
    3. `replace(/--/g, '-')` — double-dash → dash (runs AFTER newline collapse to avoid `\n--` edge)
    4. `replace(/^[,\s]+/, '')` — strip leading commas and whitespace
    5. `replace(/[,\s]+$/, '')` — strip trailing commas and whitespace
    6. `.trim()` — final trim

    Then create `src/domain/prompt/serialize.ts`:
    Import `sanitize` from `'./sanitize'` and type `PromptDraft` from `'./model'`.
    Export function `serialize(draft: PromptDraft): string`.
    Build a `parts: string[]` array:
    - Per D-01: sanitize and push `draft.intent.trim()` first (if non-empty after sanitizing)
    - Per D-01, D-03: iterate `draft.chips` in array order; for each chip where `chip.enabled` is true, sanitize `chip.label.trim()` and push if non-empty. Do NOT split intent on commas.
    - Phase 2 extension point: `draft.flags` is iterated here in Phase 2; skip for now (array is always empty in Phase 1)
    - Per D-02: return `parts.join(', ')`

    Determinism guarantee: no `Date.now()`, no `Math.random()`, no Map iteration. The function is a pure mapping over the draft's array structure — same input, same output.

    Run all three domain tests to GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/domain/prompt/ --reporter=verbose</automated>
    All three test files (model, sanitize, serialize) pass. Zero todos. Zero failures.
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/prompt/` exits 0 with all domain tests green
    - `sanitize.ts` exports only `sanitize`; no imports except built-ins
    - `serialize.ts` exports only `serialize`; imports only `sanitize` and `PromptDraft` type — no React, no Zustand
    - `sanitize('\n--ar 1:1')` returns `' -ar 1:1'` (verified by test — confirms correct operation order)
    - `serialize()` with `chips: [{ label: 'a', enabled: true }, { label: 'b', enabled: false }]` includes only 'a' in output
    - `serialize({ intent: 'cats, dogs', chips: [] })` returns `'cats, dogs'` (D-03: intent commas preserved)
    - `npx vitest run src/domain --coverage` shows 100% branch coverage on `src/domain/**`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → sanitize() → serialize() | User-typed text (intent, chip labels) enters the domain core at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `sanitize()` — MJ syntax injection via `::` or `--` in user text | mitigate | `sanitize()` collapses both to single chars; table-driven tests verify each case including the `\n--` ordering edge case |
| T-02-02 | Tampering | `serialize()` — raw user text bypassing `sanitize()` | mitigate | `serialize()` calls `sanitize()` on both `draft.intent` and each `chip.label` before pushing to parts; there is no code path into parts that bypasses sanitize |
| T-02-03 | Tampering | Future Phase 4 AI content bypassing chokepoint | mitigate | `sanitize()` is exported as a standalone function with the explicit contract (D-08) that all strings entering the serializer pass through here; Phase 4 plan must call sanitize on AI-returned chip labels via the same addChip path |
| T-02-04 | Information Disclosure | Test output logging full prompt strings | accept | Tests are developer-only; prompt strings contain no secrets in Phase 1; no telemetry |
</threat_model>

<verification>
After both tasks:
- `npx vitest run src/domain/` exits 0 — all 3 test files green, all 15+ assertions passing
- `npx vitest run src/domain --coverage` shows 100% branch coverage on src/domain/**
- model.ts, sanitize.ts, serialize.ts have zero imports from React, Zustand, or any UI framework
- `grep -rn "dangerouslySetInnerHTML" src/domain/` returns zero results
</verification>

<success_criteria>
- `PromptDraftSchema`, `sanitize()`, and `serialize()` implemented and tested
- All domain test files green with real assertions (no test.todo remaining in domain files)
- `sanitize()` correctly handles all 10 input cases from VALIDATION.md golden table
- `serialize()` correctly handles all 7 output cases including disabled-chip exclusion and D-03 intent comma preservation
- 100% branch coverage on src/domain/** confirmed via `npx vitest run --coverage`
</success_criteria>

<output>
Create `.planning/phases/01-manual-prompt-builder/01-02-SUMMARY.md` when done.
</output>

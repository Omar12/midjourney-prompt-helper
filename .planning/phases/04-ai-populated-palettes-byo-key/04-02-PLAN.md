---
phase: 04-ai-populated-palettes-byo-key
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useKeyStorage.ts
  - src/hooks/useKeyStorage.test.ts
  - src/state/buildSession.ts
  - src/state/buildSession.test.ts
autonomous: true
requirements:
  - BLD-02
  - KEY-01
  - KEY-02
  - KEY-03

must_haves:
  truths:
    - "useKeyStorage.saveKey writes the key to localStorage under 'mj-ph-api-key'"
    - "useKeyStorage.clearKey removes 'mj-ph-api-key' from localStorage and resets state to empty string"
    - "useKeyStorage initializes from localStorage using a lazy initializer, not module-level code (RESEARCH Pitfall 4)"
    - "addPaletteChip adds a chip with source:'palette' and the provided paletteCategory"
    - "addPaletteChip deduplicates by sanitized label — same label twice results in one chip"
    - "addPaletteChip routes through the sanitize() chokepoint before storing (XSS prevention)"
    - "addPaletteChip does NOT modify existing addChip behavior — existing tests still pass"
  artifacts:
    - path: "src/hooks/useKeyStorage.ts"
      provides: "useKeyStorage hook with key state, saveKey, clearKey"
      contains: "localStorage"
    - path: "src/hooks/useKeyStorage.test.ts"
      provides: "localStorage round-trip tests"
    - path: "src/state/buildSession.ts"
      provides: "addPaletteChip method added to interface and implementation"
      contains: "addPaletteChip"
    - path: "src/state/buildSession.test.ts"
      provides: "addPaletteChip test suite"
      contains: "addPaletteChip"
  key_links:
    - from: "src/hooks/useKeyStorage.ts"
      to: "localStorage API"
      via: "localStorage.getItem / setItem / removeItem"
      pattern: "mj-ph-api-key"
    - from: "src/state/buildSession.ts addPaletteChip"
      to: "src/domain/prompt/sanitize.ts"
      via: "sanitize() call before chip storage"
      pattern: "sanitize("
---

<objective>
## Phase Goal

**As an** image creator, **I want to** enter my intent and my own LLM API key to trigger AI-suggested palette options, **so that** I can assemble a complete Midjourney prompt faster than writing it by hand.

**This plan (parallel with 04-01):** Build the two state primitives that the UI will wire against — the key storage hook (localStorage read/write/clear) and the palette-chip promotion path (addPaletteChip on buildSession). Neither depends on the domain/ai types from 04-01; both can be developed and tested in isolation.

Purpose: Establish the key storage contract (KEY-01/02/03) and the chip-promotion path (BLD-02) before any UI code references them. The addPaletteChip method must be visible and tested so Plan 04-04's PaletteOption component can call it without guessing at the signature.

Output: src/hooks/ directory (new) with useKeyStorage hook + tests; buildSession.ts extended with addPaletteChip + corresponding tests.
</objective>

<execution_context>
@/Users/omar/projects/midjourney_prompt_helper/.planning/phases/04-ai-populated-palettes-byo-key/04-RESEARCH.md
@/Users/omar/projects/midjourney_prompt_helper/.planning/phases/04-ai-populated-palettes-byo-key/04-CONTEXT.md
@/Users/omar/projects/midjourney_prompt_helper/.planning/phases/04-ai-populated-palettes-byo-key/04-PATTERNS.md
</execution_context>

<context>
@/Users/omar/projects/midjourney_prompt_helper/.planning/PROJECT.md
@/Users/omar/projects/midjourney_prompt_helper/.planning/ROADMAP.md

<interfaces>
<!-- Key contracts extracted from the codebase for executor context. -->

From src/state/buildSession.ts — existing addChip to mirror (DO NOT change):
  addChip: (label) => {
    const trimmed = label.trim()
    if (!trimmed) return
    const sanitized = sanitize(trimmed)
    if (!sanitized) return
    const { chips } = get()
    if (chips.some((c) => c.label === sanitized)) return
    set({ chips: [...chips, { id: crypto.randomUUID(), label: sanitized, source: 'custom', enabled: true }] })
  }

New method to add (addPaletteChip) — interface entry AND implementation:
  interface: addPaletteChip(label: string, category: string): void
  implementation: same sanitize + dedup gate as addChip, but source:'palette' as const, paletteCategory: category

From src/domain/prompt/model.ts — ChipSchema already has:
  source: z.enum(['custom', 'palette'])
  paletteCategory: z.string().optional()

From src/state/buildSession.test.ts (lines 1-12) — reset pattern:
  beforeEach(() => {
    useBuildSession.setState({ intent: '', chips: [], selectedVersionId: null, flagValues: {}, setFlags: {} })
  })

From src/ui/ControlsPane/ChipInput.tsx (lines 1-13) — useState + handler analog for useKeyStorage:
  const [value, setValue] = useState('')

localStorage key constant: 'mj-ph-api-key'
Lazy initializer pattern (RESEARCH Pitfall 4 — do NOT read at module level):
  const [key, setKeyState] = useState(() => localStorage.getItem('mj-ph-api-key') ?? '')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useKeyStorage hook — localStorage read/write/clear with lazy init and tests</name>
  <files>src/hooks/useKeyStorage.ts, src/hooks/useKeyStorage.test.ts</files>
  <read_first>
    - src/ui/ControlsPane/ChipInput.tsx — useState + handler pattern (closest analog for hook structure)
    - src/state/buildSession.test.ts lines 1-12 — beforeEach reset pattern to apply to localStorage.removeItem
    - .planning/phases/04-ai-populated-palettes-byo-key/04-RESEARCH.md §"Pattern for useKeyStorage", §"Pitfall 4: localStorage Key Read Race During Initialization", §"localStorage Key Storage" code example
    - .planning/phases/04-ai-populated-palettes-byo-key/04-PATTERNS.md §"src/hooks/useKeyStorage.ts"
  </read_first>
  <behavior>
    useKeyStorage.ts:
    - Exports function useKeyStorage() returning { key: string, saveKey: (value: string) => void, clearKey: () => void }
    - State initialized with lazy initializer: useState(() => localStorage.getItem('mj-ph-api-key') ?? '') — NOT module-level read
    - saveKey: localStorage.setItem('mj-ph-api-key', value) + setKeyState(value)
    - clearKey: localStorage.removeItem('mj-ph-api-key') + setKeyState('')
    - No console.log of key value at any point (CLAUDE.md security requirement)

    useKeyStorage.test.ts:
    - beforeEach: localStorage.removeItem('mj-ph-api-key') to reset state
    - Test 1: saveKey('sk-test') writes localStorage['mj-ph-api-key'] === 'sk-test'
    - Test 2: clearKey() removes localStorage['mj-ph-api-key'] (getItem returns null)
    - Test 3: hook initialized when localStorage already has a key — key state === that value on first render
    - Test 4: saveKey then clearKey leaves localStorage empty and hook key === ''
    - Use @testing-library/react renderHook for hook tests; happy-dom provides localStorage mock via vitest setup
  </behavior>
  <action>
    Create src/hooks/ directory (new). Create src/hooks/useKeyStorage.ts. Import only { useState } from 'react'. The localStorage key constant 'mj-ph-api-key' should be a module-level const KEY string — not inlined per call. saveKey and clearKey are functions that both update localStorage AND React state atomically. Do not expose the raw setKeyState to callers.

    Create src/hooks/useKeyStorage.test.ts. Import { renderHook, act } from '@testing-library/react'. Import useKeyStorage from './useKeyStorage'. Use beforeEach to call localStorage.removeItem(KEY). Wrap state mutations in act(). Confirm tests do not use describe nesting beyond two levels (match existing test style in buildSession.test.ts).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useKeyStorage.test.ts --reporter=verbose</automated>
  </verify>
  <done>4 tests pass; src/hooks/useKeyStorage.ts contains 'mj-ph-api-key' constant; lazy initializer pattern (useState(() => localStorage...)) is present; no module-level localStorage.getItem call exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildSession.ts — add addPaletteChip method + extend tests</name>
  <files>src/state/buildSession.ts, src/state/buildSession.test.ts</files>
  <read_first>
    - src/state/buildSession.ts — full file: existing addChip implementation to mirror exactly (lines 30-51); BuildSessionState interface to extend; clearAll to understand reset state shape
    - src/state/buildSession.test.ts — full file: existing test structure, beforeEach reset, addChip describe block to pattern new addPaletteChip describe block after
    - src/domain/prompt/model.ts — ChipSchema confirming source:'palette' and paletteCategory fields exist
    - src/domain/prompt/sanitize.ts — sanitize function signature
    - .planning/phases/04-ai-populated-palettes-byo-key/04-RESEARCH.md §"Pattern 5: addPaletteChip Extension", §"Pattern 6: D-08 Selected-State Matching"
    - .planning/phases/04-ai-populated-palettes-byo-key/04-PATTERNS.md §"src/state/buildSession.ts — MODIFY"
  </read_first>
  <behavior>
    buildSession.ts modifications (additive only — do not change any existing method):
    - Add addPaletteChip(label: string, category: string): void to BuildSessionState interface
    - Add addPaletteChip implementation in the store: trim → sanitize → dedup (chips.some(c => c.label === sanitized)) → set chips with new entry { id: crypto.randomUUID(), label: sanitized, source: 'palette' as const, paletteCategory: category, enabled: true }
    - Do NOT change addChip — existing tests must still pass unchanged

    buildSession.test.ts additions (new describe block after existing addChip tests):
    - Test 1: addPaletteChip('Cinematic', 'styleMedium') → chips contains {label:'Cinematic', source:'palette', paletteCategory:'styleMedium', enabled:true}
    - Test 2: calling addPaletteChip twice with same label → only one chip (dedup)
    - Test 3: addPaletteChip with label containing '--unsafe' → sanitize strips the '--'; stored chip has sanitized label (no injection)
    - Test 4: addPaletteChip with empty/whitespace label → chips unchanged
    - Test 5: existing addChip tests still pass (addChip adds source:'custom', not 'palette')
  </behavior>
  <action>
    Modify src/state/buildSession.ts. Add addPaletteChip to the BuildSessionState interface directly below addChip declaration. Add the implementation in the store object directly below the existing addChip implementation. Mirror addChip's sanitize + dedup logic exactly — the only difference is the chip shape: source:'palette' as const and paletteCategory: category instead of source:'custom'. The sanitize import is already present; no new imports needed.

    Modify src/state/buildSession.test.ts. Add a new describe('addPaletteChip', () => { ... }) block. Re-use the existing beforeEach reset (it already clears chips to []). Import nothing new — useBuildSession is already imported. Tests verify source:'palette', paletteCategory presence, dedup, and sanitize gate. Do not touch any existing test.
  </action>
  <verify>
    <automated>npx vitest run src/state/buildSession.test.ts --reporter=verbose</automated>
  </verify>
  <done>All existing buildSession tests still pass; new addPaletteChip describe block adds at minimum 4 passing tests; src/state/buildSession.ts contains 'addPaletteChip' in both the interface and the store implementation; source:'palette' as const present in implementation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage origin | API key written to and read from localStorage on the same browser origin; readable by any script with origin access |
| addPaletteChip label argument | AI-returned label text enters buildSession state here; sanitize() is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | useKeyStorage / localStorage | accept | Key is stored in plaintext localStorage on web v1; accepted per CLAUDE.md §Security for web build; XSS prevention is the mitigating control; user will be informed in settings modal (Plan 04-03) |
| T-04-02-02 | Tampering | addPaletteChip label | mitigate | sanitize() called on every label before chip storage — same chokepoint as addChip; MJ-syntax injection (:: or --) stripped before reaching chip state |
| T-04-02-03 | Information Disclosure | useKeyStorage saveKey | mitigate | No console.log or analytics call includes the key value; saveKey is a simple localStorage write with no network side effects |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; only React hooks and existing sanitize.ts used |
</threat_model>

<verification>
Run the targeted and full suites:

```
npx vitest run src/hooks/ src/state/buildSession.test.ts --reporter=verbose
npx vitest run
```

Both must exit 0. Confirm addPaletteChip exists in buildSession.ts with source:'palette' as const (grep). Confirm useKeyStorage.ts contains lazy initializer pattern (grep "useState(() =>"). Confirm no module-level localStorage.getItem call exists outside the lazy initializer.
</verification>

<success_criteria>
- npx vitest run exits 0 with all pre-existing tests still green
- useKeyStorage saveKey writes to 'mj-ph-api-key', clearKey removes it
- addPaletteChip adds chip with source:'palette', paletteCategory, sanitized label
- addPaletteChip dedup prevents same-label duplicates
- No change to existing addChip behavior
</success_criteria>

<output>
Create .planning/phases/04-ai-populated-palettes-byo-key/04-02-SUMMARY.md when done.
</output>

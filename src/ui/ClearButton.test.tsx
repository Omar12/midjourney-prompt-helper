import { describe, test } from 'vitest'

describe('ClearButton / ClearDialog', () => {
  test.todo('triggers confirm dialog when intent or chips are present')
  test.todo('clearing the dialog clears state')
  test.todo('no dialog appears when builder is already empty')
})

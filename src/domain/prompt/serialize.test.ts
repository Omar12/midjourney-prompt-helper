import { describe, test } from 'vitest'

describe('serialize', () => {
  test.todo('intent first then chips joined by `, `')
  test.todo('same input produces same output (deterministic)')
  test.todo('disabled chips are skipped')
  test.todo('empty draft produces empty string')
  test.todo('intent with user commas is preserved as-is (opaque block, D-03)')
})
